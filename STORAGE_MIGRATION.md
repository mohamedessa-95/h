# توحيد تخزين برنامج مكتب المحاماة

تم تجهيز طبقة التخزين الجديدة على أساس SQLite مع مجلد مستندات ونسخ احتياطية موحّد:

- `storage/office.sqlite` قاعدة البيانات المحلية.
- `storage/documents/` ملفات المستندات والتوكيلات.
- `storage/backups/` النسخ الاحتياطية.

## مهم
هذه نسخة **مجهزة للهجرة** وليست إصدارًا نهائيًا للاستخدام التجاري بعد.
قبل تشغيلها على بيانات عميل حقيقية يجب تنفيذ Migration فعلي من localStorage/IndexedDB إلى SQLite، ثم اختبار Backup/Restore، ثم استبدال نقاط القراءة والكتابة القديمة في واجهة البرنامج.

لا يتم حذف التخزين القديم تلقائيًا حتى لا يحدث فقد للبيانات.
